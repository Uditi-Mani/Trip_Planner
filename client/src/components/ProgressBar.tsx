export function ProgressBar({ completed, total }: { completed: number; total: number }) { const percent = total ? Math.round((completed / total) * 100) : 0; return <div className="flex items-center gap-3 text-sm text-pine-700 dark:text-emerald-300"><span className="font-bold">{completed} / {total}</span><span>activities complete</span><div className="h-2 w-28 overflow-hidden rounded-full bg-pine-50 dark:bg-white/10"><div className="h-full rounded-full bg-pine-500 transition-all duration-500" style={{ width: `${percent}%` }}/></div></div>; }
